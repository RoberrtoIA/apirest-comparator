<?php

namespace Tests\Unit;

use App\Models\CPU;
use Illuminate\Database\Eloquent\Collection;
use App\Models\Post;
use PHPUnit\Framework\TestCase; 
// use Tests\TestCase;

class cpuTest extends TestCase
{
    /**
     * A basic unit test example.
     *
     * @return void
     */
    public function test_CPUstore()
    {
    //     $response = $this->postJson('/store-cpu', 
    // [
    //     'CPUModel' => 'cpu',
    //     'Score' => 'score',
    //     'Speed' => 'speed',
    //     'idCPUBrand' => 'idcpubrand'
    // ]);

    // $response->assert(200);
    // $cpu = new CPU;

    // $this->assertInstanceOf(Collection::class, $cpu->posts);

        // $this->assertTrue(true);
    }
}
